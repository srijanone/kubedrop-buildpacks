# Buildpack API version
api = "0.2"

# Buildpack ID and metadata
[buildpack]
id = "kubedrop.io/drupal"
version = "0.0.1"
name = "Drupal Buildpack"

# Stacks that the buildpack will work with
[[stacks]]
id = "io.buildpacks.stacks.kubedrop.drupal"